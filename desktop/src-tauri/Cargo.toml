[package]
name = "local-studio"
version = "0.4.0"
description = "Local Studio — AI video editor chạy 100% trên máy"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
codegen-units = 1
