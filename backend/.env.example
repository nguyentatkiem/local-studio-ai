# Đổi tên file này thành .env rồi điền key. .env đã được git bỏ qua.
# Pexels (B-roll tự động) — lấy miễn phí tại https://www.pexels.com/api/
PEXELS_API_KEY=

# Model Claude cho Đạo diễn / não AI (sonnet|opus|haiku), mặc định sonnet
LS_CLAUDE_MODEL=sonnet
